# Este es un archivo de ejemplo para las variables de entorno
# Crea un archivo .env con los valores reales

# URL de conexión a MongoDB Atlas
MONGODB_URI=mongodb+srv://usuario:password@clustername.mongodb.net/database

# Puerto para el servidor web
PORT=3000

# URL del webhook de N8N (si se usa)
# N8N_WEBHOOK_URL=https://tu-instancia-n8n.com/webhook/...